# 进度与遗留（截至 2026-08-08 晚）

这份文件回答一个问题：**停在哪儿了、下一步捡哪一件。**

工作区干净，203 条测试全绿。

---

## 〇、⑤ 提速：代码接管物理与读数面板（**本轮做完**）

设计：`specs/2026-08-08-scene-codegen-design.md`　计划：`plans/2026-08-08-scene-codegen.md`

做完了六件：`scenegen.py`（表/probe/面板/figcaption/骨架）、`harness/build.py`
（拼装 + 挡绕过）、接进 ⑤ 链路（`scenes.gen`、CONTRACT v3、allow-list、哈希基线）、
播放控制（时间轴/键盘/倍速/多情形，web 与离线页两处）、实跑三道题、对抗性测试。

**实测：三道题全部第一轮通过**（q13 旧流程 3 轮），agent 写的 JS 降 47%。
**但单轮耗时没降**（复杂题仍 13.5 分钟）—— 别拿它当「⑤ 变快了」宣传，
它变的是更少失败、更少返工。

### 剩下的

- **铺开与否**：现在只跑了 3 道。要不要对全部题启用，等目检之后定。
- **「文字压图形」门禁（L6）**：判据已标定（干净组 0.27 / 乱组 1.79，2.2 倍空档），
  但**这一期没上** —— 代码生成改变了文字分布，阈值要重标。顺序是：
  生成几道新场景 → 连旧的一起人工目检 → 再标阈值。
- **`_runtime.js` 与 `SceneMount.tsx` 的时间轴没有人肉验过**。语法与门禁都过了，
  但版面挤不挤、拖起来跟不跟手，得有人看。
- **方舟后端还没接代码生成**：它的提示词还在让模型写 `<id>.js`，开了会被
  allow-list 丢掉。要用那条路得先改提示词。
- 「标签甩飞」那个盲区仍然补不上（q13-gen3 归一化只有 0.19，判据会说它干净）。

### 这一轮踩到并修掉的（都写在 commit 里）

1. 面板占位符用裸的 `__panel__` → ⑥ 的 L1 要求所有 id 带场景前缀。**对抗测试抓到的**。
2. `window.probe = ...` 这种绕过没挡住 → 骨架里 `probe` 是闭包局部变量，覆盖不了它，
   但 agent 会以为自己接管了物理，画出一套跟 probe 不一致的东西，**而 L4 仍然全过**。
3. 拼装不幂等 → 第二次跑时自己注入的面板被判成「侵入面板」。幂等测试逮到的。
4. 自动挑读数按 `probe_keys` 顺序取前 4 个太蠢 → 改按 `invariants[].report` 频次排。

---

## 一、答题卡诊断（主线，**暂停中**）

设计文档：`specs/2026-08-06-answer-sheet-diagnosis-design.md`（2026-08-08 大改过）

### 做完了

| 内容 | commit |
|---|---|
| 知识点受控词表，15 章 154 条 | `7c8c8c6` |
| `questions` 加 `kps` / `ref_answer` / `ref_answer_src` | `a1bc434` |
| ②d 从卷子里抽标准答案（纯代码）+ 全语料反幻觉门禁 | `b9eab0c` `fbcf15d` |
| ③c 整卷知识点标注，接进两条链 | `66589b7` `c39d9ab` |
| 页面显示知识点与标准答案 | `dcbb182` |
| 进度里加 ③c 那一格 | `af028c3` |
| `pages.py` 一批文件 → 规范化页面图 | `27b676e` |
| `answer_sheets` / `sheet_answers` 两张表 | `bfa38c5` |
| `grade.py` 判对错归一化（57 条测试） | `d9033fa` |
| `source_kind` + `ref_solution` 两列、建卷与读写 | `facbc4d` |
| Ⓐ 读参考答案 | `2349659` |
| `stage_of` 按 `source_kind` 分支 | `09aba4a` |

**Ⓐ 已经拿老师的真材料实跑过三轮**，26 题入库、题号无矛盾。

### 没做完

计划：`plans/2026-08-08-answer-only-papers.md`

| 任务 | 状态 | 卡在哪 |
|---|---|---|
| Task 2b　Ⓔ 读题干 | 未开始 | **等老师的题目图片** |
| Task 4　③c 换输入（题干 + 官方解答） | 未开始 | 代码可写，验收要 2b |
| Task 5　上传入口 `POST /api/answer-papers` | 未开始 | — |
| Task 6　页面（没有题干时的版面、官方解答出处） | 未开始 | — |

再往后（还没出计划）：

- **步二**：Ⓢ 从手机截图里抠答题卡 → Ⓑ 读批改结果 → Ⓒ 判定与互校 → 改判
- **步三**：Ⓓ 薄弱知识点与提升建议

`plans/2026-08-08-answer-sheet-grading.md` 是**改设计之前**写的答题卡计划。
其中 Task 2/3/4（`pages.py`、两张表、`grade.py`）已完成且仍然有效；
**Task 1、5-10 已被新设计取代**（不再自动阅卷，对错以老师的批改为准），
动步二之前要按新设计重出。

---

## 二、外部依赖（不给就走不动）

1. **题目图片** —— 学生做的那份卷子拍几张。挡着 Task 2b/4/5/6 的验收。
   没有它，13 道题（含全部 8 道选择题）挂不上知识点，诊断只能覆盖 3 道大题。
2. **手写探针要重跑** —— 步二开工前，用**只裁不放大**的答题卡图重跑一次
   （第一次探针喂了放大 3 倍的图，600 秒超时，见下面「已知坑」第 2 条）。

---

## 三、已知问题（都还没修）

| # | 问题 | 严重性 |
|---|---|---|
| 1 | **`15(3)` 三轮都没读出来。** 题号「(3)」印在第 2 页页脚、答案 `0<θ<π/4` 在第 3 页页首，被页边界劈成两半 | 低 —— 是「漏」不是「错」，Ⓔ 和 Ⓑ 的对账会发现它 |
| 2 | `16(1)` 的答案是 `E = BLv₀ / I₁ = 3mg/(5BL)`，用 ` / ` 串了两个结果，跟 `grade.split_blanks` 的多空语义撞 | 中 —— 步二互校时会误判 |
| 3 | 13 个旧场景过不了 ⑥ 的 L5 版面层 | 低 —— 不影响已上线的图，没定要不要分批重做 |
| 4 | `q16-gen2` 过不了 2 条 L4 物理断言 | 低 —— 既有问题 |
| 5 | **上传撞名的 409、以及上传句柄恢复，一直没在页面上验过** | 中 —— 代码写了，没验 |

---

## 四、踩过的坑（写下来免得重踩）

1. **`stage_of` 加一格必须同时改分支/回填**，否则已完成的卷子集体退回「未完成」、
   进度带永远转。踩过两次（③c、`answers_only`）。
2. **不要把图放大后再送模型。** 同一页 1080×1441 要 57 秒，放大到 1620×2208
   之后 claude CLI 600 秒都回不来。要看清就裁小一块，别整张放大。
3. **JSON 会静默吃掉 LaTeX 反斜杠。** `\t` `\f` `\r` `\b` 都是合法 JSON 转义，
   `json.loads` 会把 `\times` 变成 TAB+"imes"、`\frac` 变成 FF+"rac"。不报错。
   已在 `mathvlm.loads_lenient` 修掉（`bd46b2c`）。
4. **`index.html` 必须 `no-cache`。** 否则前端更新了页面毫无变化，普通刷新也没用 ——
   它里面写着当前那版 JS 的文件名。已修（`ccdbd42`）。
5. **upsert 只更新自己那几列。** `put_answer_question` / `put_stem` / `put_kps`
   三步分工写死，列错一个就会在重跑上游时把下游的产出冲没。
6. **带圈数字/字母不是 Python 合法标识符**，测试函数名里不能写 `③` `Ⓐ`。
7. **子进程的 `print` 是块缓冲**，跑几分钟的脚本不 `flush` 的话外面一个字都看不到，
   跟卡死分不出来。

---

## 五、怎么恢复现场

```bash
cd /Users/jerry/Desktop/product/exam-explainer
.venv/bin/pytest tests/ -q                    # 应该 203 passed
.venv/bin/uvicorn pipeline.api:app --host 127.0.0.1 --port 8712 >> logs/api.log 2>&1 &
```

老师给的真实材料在 `~/Desktop/试卷+答题卡/`（4 张参考答案 + 2 张已批改答题卡截图）。
已经跑出来的卷子叫 `2025-2026高二物理期末`（`source_kind='answers_only'`，26 题）。
