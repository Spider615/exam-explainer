# 进度与遗留（截至 2026-08-08 晚）

这份文件回答一个问题：**停在哪儿了、下一步捡哪一件。**

工作区干净，**508 条测试全绿**（2026-08-09 实测；早先写的 294 / 265 都已过期）。

---

## 〇、两种模式：解析试卷 · 答题卡诊断（**本轮做完**）

设计：`specs/2026-08-09-two-modes-design.md`　计划：`plans/2026-08-09-two-modes.md`
分支 `two-modes`，13 个任务，一任务一 commit。

**页面上多了两个 tab。** 切过去整屏都换：上传框、库列表的列头、详情页。
两个模式互相看不见对方的卷子（`?mode=paper|sheet`），地址分两支
（`#/paper/...` / `#/sheet/...`），老地址 `#/p/<卷名>` 落地后会问一次 `/progress`
再跳到它真正属于的模式。

**答题卡这条链第一次在页面上跑得通**：`POST /api/answer-papers` 收「卷名 + 一批
参考答案图」→ Ⓐ 读参考答案 → ③c 挂知识点。**已用老师那 4 张真图端到端跑过**
（见下）。

### 这一轮真正解决的结构问题

「一共有哪几步、每步叫什么」原来抄了**三份**（`api.stage_of` 的一串 return、
前端 `STAGE_LABEL`、前端 `STAGE_OF_CODE`），历史上那两次事故 ——「进度带永远转」
和「一步全程一格都不亮」—— 都是抄漏了一份。

现在只有 `pipeline/modes.py` 一份，而且**每一格的状态也由后端算完再下发**
（`modes.cell_states`）。前端 `PaperView.tsx` 里 `STAGE_LABEL` / `STAGE_OF_CODE` /
`NEEDS_ARTIFACT` / `stageStates` **四样一起删了**，只剩 `map` 一遍照着画。

搬到后端的理由是具体的：**`web/` 一个测试都没有**（508 条全是 Python），而这段
判断恰恰是出过两次错的那段。本轮**不引入前端测试框架** —— 那是另一个决定，不夹带。

配了一条从源码扫代号的门禁（`tests/test_modes.py`）：`stage_of` 会返回的每个代号
都必须在该模式的格子表里登记过。**它在「往管线里加一步」那一刻触发**，正是历史上
出事的时刻。跨语言那条旧门禁 `test_stage_code_covered.py` 被它取代后删掉了。

### 端到端实跑（2026-08-09）

传老师那 4 张参考答案图（1080×1441），4 分钟读出 26 题。**逐题对人工真值表 4/4**：

| 题 | 真值 | 实际 |
|---|---|---|
| 9 | `不变 / 17190` | ✓ |
| 11 | `2BIL / MP` | ✓ |
| 12(1) | `170 / A` | ✓ |
| 13(1) | `BC` | ✓ |

护栏也验了：卷名填一份已有解析试卷的名字 → 自动改成「… (2)」，**那份卷子的
`source_kind` 还是 `pdf`，没被就地改掉**。

跑出来的卷子叫 `端到端验收卷`（26 题），留在库里没删 —— 想清掉从页面上删就行。

### 顺手补掉的五个洞（都是既有的，不补这一轮走不通）

1. **`GET /api/papers/<答案卷>` 一直是 500**。答案卷的 `layout` 是空对象，
   `figures` / `fig_marks` 两个键**根本不存在**，而 `api.paper` 那句 `x["figures"]`
   是硬取 —— 答题卡详情页从来就打不开。**它躲过了全部测试，因为这个端点现有的
   测试全是打桩的、人手写的桩键是齐的**。补的那条走真库。
2. **`ref_solution`（官方解答过程）到不了页面**。`get_paper` 查了这一列，投影里漏了。
   它是「答题卡诊断」最值钱的产出，在库里躺着没人看得见。
3. **答题卡这条链建不出新卷子**：`create_answers_paper` 全仓库只有测试在调。
   `refread.py` 加了 `--create`（**显式**建，不许隐式 —— 隐式建的话卷名打错一个字
   就凭空多出一份新卷子）。
4. **`PIPE_RE` 认不出 `refread`**：「这份卷子在跑吗」对答题卡链永远答 false，
   上传闸门是漏的。顺带发现卷名边界正则也漏判 refread 的「卷名 + 一串文件名」，
   改成 `\s+-|\s+(?!\()\S|$`（`\S` 那一位堵住了多空格回溯，不再依赖
   `free_name` 只加一个空格这个别处的约定）。
5. **`create_answers_paper` 会静默把一份解析试卷改成答题卡卷子**。今天只有测试
   在调所以没出事；接上上传入口就成真了 —— 填一个和某份高考真题重名的卷名，
   那份跑了一小时的卷子当场换模式，解法和动画还在库里却一格都不显示，
   **一句提示都没有**。加了 `ON CONFLICT ... WHERE source_kind='answers_only'`。

---

## 〇之前：三个阶段搬到火山方舟

实测存档：`specs/2026-08-08-scene-speed-and-backends.md`（数字都在那里）

| 阶段 | 走哪 | 思考 | 怎么走 |
|---|---|---|---|
| ②b 公式识别 | 方舟 `doubao-seed-evolving` | — | 直连，图 base64 内联 |
| ④ 写 spec | 方舟 DeepSeek-V4-Flash | 开 | 直连（`EXAM_SPEC_BACKEND=http`） |
| ⑤ 生成动画 | 方舟 DeepSeek-V4-Flash | 关 | claude CLI 经 arkshim |

**订阅额度从此一点都不用。** 三个阶段里只有 ⑤ 经过垫片，所以
`EXAM_ARK_THINKING=disabled` 不会误伤要开思考的 ④。

### 一定要知道的代价

**④ 换成 DeepSeek 之后断言少了四到六成**（q17 15→6、q13 22→14，n=2）。
断言就是 ⑥ 的 L4 那一层 —— **spec 弱，门禁就弱**，而 ④b 只检自洽、检不出
「不够严」。另外它会把 sonnet 认为能做的题判成「不适合做动画」（北京卷 q5），
这是**静默减产**，没人会来报错。要不要为此加一道门禁，是个待定的产品决定。

### 顺带修掉的两个隐患

1. **`.env` 改了对页面不生效** —— 后端进程启动时把 .env 烤进了 `os.environ`，
   又原样传给管线子进程。配置落好、测试全绿、页面上跑的还是订阅。已修。
2. **垫片复用不核对身份** —— 思考开关和 key 是进程启动时读死的，
   旧垫片会被静默复用。已修（`GET /` 出身份牌）。

---

## 〇、⑤ 提速：代码接管物理与读数面板（**本轮做完**）

设计：`specs/2026-08-08-scene-codegen-design.md`　计划：`plans/2026-08-08-scene-codegen.md`

做完了六件：`scenegen.py`（表/probe/面板/figcaption/骨架）、`harness/build.py`
（拼装 + 挡绕过）、接进 ⑤ 链路（`scenes.gen`、CONTRACT v3、allow-list、哈希基线）、
播放控制（时间轴/键盘/倍速/多情形，web 与离线页两处）、实跑三道题、对抗性测试。

**验收三项已有结论**：轮数达标（实测 3 道 + 真实卷子 2 道，**五道全部第一轮通过**）、
人工目检达标（老师看过真实卷子，「没有压字了」「时间轴挺好」）、
**单轮耗时没达标**（复杂题仍 13.5 分钟）—— 别拿它当「⑤ 变快了」宣传。

**「铺开」不是待办**：`can_codegen` 是无条件判的，每份新卷子已经在走这条路。
刚上传的 2025 黑龙江卷两道动画都是 `gen=codegen`，1 轮 PASS。

### 剩下的

- **L6（文字压图形门禁）可以上，但不紧急**。新场景实测 0.00–0.27（旧干净组 0.27、
  旧乱组 1.79），阈值候选 0.8 余量很大。它的价值已从「抓现有问题」变成「防回归」。
- **「标签甩飞」盲区仍在**：q13-cg1 测出 0.00，是真干净还是标签甩进了空白处，
  没有人看过那张图。
- **方舟后端还没接代码生成**：它的提示词还在让模型写 `<id>.js`，开了会被
  allow-list 丢掉。要用那条路得先改提示词。
- 「标签甩飞」那个盲区仍然补不上（q13-gen3 归一化只有 0.19，判据会说它干净）。

### 这一轮踩到并修掉的（都写在 commit 里）

1. 面板占位符用裸的 `__panel__` → ⑥ 的 L1 要求所有 id 带场景前缀。**对抗测试抓到的**。
2. `window.probe = ...` 这种绕过没挡住 → 骨架里 `probe` 是闭包局部变量，覆盖不了它，
   但 agent 会以为自己接管了物理，画出一套跟 probe 不一致的东西，**而 L4 仍然全过**。
3. 拼装不幂等 → 第二次跑时自己注入的面板被判成「侵入面板」。幂等测试逮到的。
4. 自动挑读数按 `probe_keys` 顺序取前 4 个太蠢 → 改按 `invariants[].report` 频次排。

---

## 一、答题卡诊断（主线，**暂停中**）

设计文档：`specs/2026-08-06-answer-sheet-diagnosis-design.md`（2026-08-08 大改过）

### 做完了

| 内容 | commit |
|---|---|
| 知识点受控词表，15 章 154 条 | `7c8c8c6` |
| `questions` 加 `kps` / `ref_answer` / `ref_answer_src` | `a1bc434` |
| ②d 从卷子里抽标准答案（纯代码）+ 全语料反幻觉门禁 | `b9eab0c` `fbcf15d` |
| ③c 整卷知识点标注，接进两条链 | `66589b7` `c39d9ab` |
| 页面显示知识点与标准答案 | `dcbb182` |
| 进度里加 ③c 那一格 | `af028c3` |
| `pages.py` 一批文件 → 规范化页面图 | `27b676e` |
| `answer_sheets` / `sheet_answers` 两张表 | `bfa38c5` |
| `grade.py` 判对错归一化（57 条测试） | `d9033fa` |
| `source_kind` + `ref_solution` 两列、建卷与读写 | `facbc4d` |
| Ⓐ 读参考答案 | `2349659` |
| `stage_of` 按 `source_kind` 分支 | `09aba4a` |

**Ⓐ 已经拿老师的真材料实跑过三轮**，26 题入库、题号无矛盾。

### 没做完

计划：`plans/2026-08-08-answer-only-papers.md`

| 任务 | 状态 | 卡在哪 |
|---|---|---|
| Task 2b　Ⓔ 读题干 | 未开始 | **等老师的题目图片** |
| Task 4　③c 换输入（题干 + 官方解答） | 未开始 | 代码可写，验收要 2b |
| Task 5　上传入口 `POST /api/answer-papers` | **已完成**（2026-08-09） | — |
| Task 6　页面（没有题干时的版面、官方解答出处） | **做了一半** | 版面有了（标准答案 · 官方解答 · 知识点，三种「没有」各说各的话）；「官方解答出处」还没做 |

再往后（还没出计划）：

- **步二**：Ⓢ 从手机截图里抠答题卡 → Ⓑ 读批改结果 → Ⓒ 判定与互校 → 改判
- **步三**：Ⓓ 薄弱知识点与提升建议

`plans/2026-08-08-answer-sheet-grading.md` 是**改设计之前**写的答题卡计划。
其中 Task 2/3/4（`pages.py`、两张表、`grade.py`）已完成且仍然有效；
**Task 1、5-10 已被新设计取代**（不再自动阅卷，对错以老师的批改为准），
动步二之前要按新设计重出。

---

## 二、外部依赖（不给就走不动）

1. **题目图片** —— 学生做的那份卷子拍几张。挡着 Task 2b/4/5/6 的验收。
   没有它，13 道题（含全部 8 道选择题）挂不上知识点，诊断只能覆盖 3 道大题。
2. **手写探针要重跑** —— 步二开工前，用**只裁不放大**的答题卡图重跑一次
   （第一次探针喂了放大 3 倍的图，600 秒超时，见下面「已知坑」第 2 条）。

---

## 三、已知问题（都还没修）

| # | 问题 | 严重性 |
|---|---|---|
| 1 | **`15(3)` 三轮都没读出来。** 题号「(3)」印在第 2 页页脚、答案 `0<θ<π/4` 在第 3 页页首，被页边界劈成两半 | 低 —— 是「漏」不是「错」，Ⓔ 和 Ⓑ 的对账会发现它 |
| 2 | `16(1)` 的答案是 `E = BLv₀ / I₁ = 3mg/(5BL)`，用 ` / ` 串了两个结果，跟 `grade.split_blanks` 的多空语义撞 | 中 —— 步二互校时会误判 |
| 3 | 13 个旧场景过不了 ⑥ 的 L5 版面层 | 低 —— 不影响已上线的图，没定要不要分批重做 |
| 4 | `q16-gen2` 过不了 2 条 L4 物理断言 | 低 —— 既有问题 |
| 5 | **上传撞名的 409、以及上传句柄恢复，一直没在页面上验过** | 中 —— 代码写了，没验 |
| 6 | **图形选项题的选项图认不出来**（详见下条） | 高 —— 6/11 道这类题是坏的，正在修 |
| 7 | **答题卡模式里还没有答题卡**（详见下条） | 中 —— 名字先立住了，内容是空的 |
| 8 | **③c 只挂得上大题，选择填空全挂不上**（详见下条） | 中 —— 卡在老师的题目图上 |
| 9 | **答案卷永远到不了「已完成」**（详见下条） | 低 —— 显示是诚实的，只是难看 |

### 问题 7：这个模式里还没有答题卡

「答题卡诊断」这一轮只做到**参考答案**那一半（Ⓐ 读参考答案 + ③c 挂知识点）。
学生的答题卡（Ⓢ 抠图 / Ⓑ 读批改 / Ⓒ 判定互校）和诊断报告（Ⓓ）都还没做 ——
就是设计文档里的**步二、步三**。名字与内容暂时不完全相符，这是有意的：
改名比改数据便宜，等步二落地再看要不要调。

### 问题 8：③c 挂得上 9/26，剩下 17 道要等 Ⓔ

2026-08-09 实跑：`端到端验收卷` 26 题，③c 挂上 **9 题**，全是有官方解答过程
（`ref_solution`）的大题；剩下 17 道是选择题和填空题，答案只有一个「D」或
「不变 / 17190」。

**这不是 bug，是设计文档预言过的**：「一个孤零零的『D』推不出这道题考什么。
没有题干的话，13 道题（含全部 8 道选择题）挂不上知识点」—— 这正是 Ⓔ 读题干
存在的理由，而 Ⓔ 卡在老师还没给的题目图片上（见「外部依赖」第 1 条）。

顺带记一笔：这一轮**修掉了 ③c 在这条链上完全空跑的问题**（改之前是 0/26）。
`kpmark.payload_for` 原来只读题干和 ③ 的解法，送过去的是 26 段只有题号的空白。
现在答题卡这条链改送「标准答案 + 官方解答过程」，解析试卷那条链一个字节没变。

### 问题 9：答案卷的进度永远停在「③c 知识点 9/26 · 已停止」

`_stage_of_sheet` 判「做完了」用的是 `kps >= 题数`。而上面那 17 道题**现在
就是挂不上**，所以这份卷子永远到不了「已完成」，点「继续执行」也只会再挂 9 道。

**显示是诚实的**（③c 确实没覆盖全部题目），但它和这个仓库自己的教训撞了：
`stage_of` 的注释里写着「⑤ 的分子必须是『试过几道』而不是『绿灯几道』——
有一道怎么都过不了门禁的话，按绿灯数算就永远差一个，永远显示在跑」。
③c 这里用的正是被否掉的那种数法。

**没有当场改，因为改它需要一个「③c 跑过了」的记录**，而库里没有这个信号
（`kps` 为空既可能是没跑过、也可能是跑了没挂上）。加这个信号是改表，
不该顺手夹带。**动 Ⓔ 的时候一起定**：要么加一列，要么让 Ⓔ 落地后 17 道自然挂上。

### 问题 6 的诊断（`group_option_figures` 只认单行）

选项本身是图的题（「下列四幅电场线图哪个正确」），四张图要分给 A/B/C/D。
`segment.py` 的 `group_option_figures` 只认「**一行 N 个并排**」，认不出
**2×2 网格**，而 2×2 在高考卷里很常见。另外它按 `round(y/12)` 分行也是错的：
记录里 `y` 是图片**底边**，同一行的图往往顶边齐而底边不齐（陕西卷第8题两张并排图
底边差 18，分进了两个桶，顶边却完全相同）。

全库 9 份卷子：这类题 11 道，认出来 5 道、**坏了 6 道**。

坏了之后：`text_quality` 判 degraded → 页面上选项 A/C/D 全空、四张图无标号挂在
题干下面，**读题的人分不出哪张图是哪个选项**；③ 解题只拿到「题干插图1..4」没有
字母，只能靠数数猜对应。

**已核实：陕西卷 q1/q8 两道答案都对**，模型是靠看图内容补回来的（认出 y-t 振动
图像不是选项，从第二张开始数）。但那依赖两件没有保障的事：图的顺序恰好等于阅读
顺序（`ingest.extract_page_images` 遍历 `page.images` **没有排序**，拿的是 PDF
内容流顺序），以及题干图和选项图长得不一样。一旦错位就是整道题全错，而且
**静默** —— 物理推理照样完美，只有字母错；真题 PDF 没有参考答案对不了账，
④b 只检 spec 自洽、⑥ 只检动画，**全链路没有一层看选项字母**。

---

## 四、踩过的坑（写下来免得重踩）

1. **`stage_of` 加一格必须同时改分支/回填**，否则已完成的卷子集体退回「未完成」、
   进度带永远转。踩过两次（③c、`answers_only`）。
2. **不要把图放大后再送模型。** 同一页 1080×1441 要 57 秒，放大到 1620×2208
   之后 claude CLI 600 秒都回不来。要看清就裁小一块，别整张放大。
3. **JSON 会静默吃掉 LaTeX 反斜杠。** `\t` `\f` `\r` `\b` 都是合法 JSON 转义，
   `json.loads` 会把 `\times` 变成 TAB+"imes"、`\frac` 变成 FF+"rac"。不报错。
   已在 `mathvlm.loads_lenient` 修掉（`bd46b2c`）。
4. **`index.html` 必须 `no-cache`。** 否则前端更新了页面毫无变化，普通刷新也没用 ——
   它里面写着当前那版 JS 的文件名。已修（`ccdbd42`）。
5. **upsert 只更新自己那几列。** `put_answer_question` / `put_stem` / `put_kps`
   三步分工写死，列错一个就会在重跑上游时把下游的产出冲没。
6. **带圈数字/字母不是 Python 合法标识符**，测试函数名里不能写 `③` `Ⓐ`。
7. **子进程的 `print` 是块缓冲**，跑几分钟的脚本不 `flush` 的话外面一个字都看不到，
   跟卡死分不出来。
8. **`.env` 是进程启动时读的，而且是 `setdefault`。** 后端把自己那份快照原样传给
   管线子进程，子进程改不动。已修，但同类问题（模块顶层读环境变量的常量）到处都是，
   改配置之后别只看文件，要看**进程实际解析出来的值**。
9. **代码明明是对的却报 `KeyError` / `AttributeError`，先清 `__pycache__`。**
   traceback 显示的是新源码、跑的可能是旧字节码，两边对不上时人会一直怀疑源码。

---

## 五、怎么恢复现场

```bash
cd /Users/jerry/Desktop/product/exam-explainer
.venv/bin/python -m pytest tests/ -q          # 应该 508 passed
.venv/bin/uvicorn pipeline.api:app --host 127.0.0.1 --port 8712 >> logs/api.log 2>&1 &
```

老师给的真实材料在 `~/Desktop/试卷+答题卡/`（4 张参考答案 + 2 张已批改答题卡截图）。
已经跑出来的卷子叫 `2025-2026高二物理期末`（`source_kind='answers_only'`，26 题）。
